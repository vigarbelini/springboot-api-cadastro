# springboot-api-cadastro
API REST básica desenvolvida com Java e Spring Boot para gerenciamento de usuários, seguindo arquitetura em camadas e implementando operações completas de CRUD. Utiliza Spring Data JPA (Hibernate) para mapeamento objeto-relacional e MySQL para persistência de dados, aplicando boas práticas de organização, e estruturação do backend.
